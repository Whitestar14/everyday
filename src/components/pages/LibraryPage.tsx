"use client"

// Library view is removed. Keep a lightweight stub to prevent import failures while route references are removed.
export function LibraryPage() {
  console.warn("Library page has been removed. Use Manage tasks instead.")
  return null
}
